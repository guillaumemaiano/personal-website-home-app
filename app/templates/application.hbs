<div class="container">
    <nav class="menu navbar navbar-inverse">
        <div class="container-fluid">
            <div class="navbar-header"><!-- home and mobile button -->
                {{#link-to 'index' class ="navbar-brand text-capitalize"}}
                <em>{{t "home"}}</em>
                {{/link-to}}<!-- FIXME -->
                <button type="button" class="navbar-toggle" data-toggle="collapse" onclick={{action 'navBarClicked' value='target'}}>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span> 
                </button>
            </div><!-- /.navbar-header -->
            <div class="collapse navbar-collapse">
                <ul class="nav navbar-nav">
                    <li class="links">
                        {{#link-to 'news'}}
                        {{t 'news'}}
                        {{/link-to}}</li>
                    <li class="links">
                        {{#link-to 'private'}}
                        {{t 'private'}}
                        {{/link-to}}
                    </li>
                </ul>
                <ul class="nav navbar-nav navbar-right">
                    <li>
                        {{#link-to 'private' class='text-right'}}<span class="glyphicon glyphicon-log-in text-capitalize"> {{t 'login'}}</span>{{/link-to}}
                    </li>
                </ul>
            </div><!-- /.navbar-collapse -->
        </div><!-- /.container-fluid -->
    </nav>
    <div class="body">
        {{outlet}}
    </div>
</div>
<!-- full size footer outside of container -->
<footer class="footer">
    <div class="container">
        <div class="row text-center">
            <div class="text-muted mx-auto col-lg-12 col-md-12 col-sm-12 col-xs-12">&copy; Guillaume Maiano 2017</div>
        </div>
    </div>
</footer>
